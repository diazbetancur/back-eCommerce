# ========================================
# JWT CONFIGURATION (REQUIRED)
# ========================================

# Signing key for JWT tokens (MINIMUM 32 characters)
# ⚠️ NEVER commit real key to Git - use environment variables in production
Jwt__SigningKey=your-secret-key-minimum-32-characters-long-change-this-in-production

# Issuer identifier (who generates the token)
Jwt__Issuer=ecommerce-api

# Audience identifier (who the token is for)
Jwt__Audience=ecommerce-clients

# Strict validation mode (true for Production, false for Development)
# Production: true  → Rejects tokens without issuer/audience
# Development: false → Accepts legacy tokens (backward compatibility)
Jwt__StrictValidation=true

# Token expiration time in minutes
Jwt__ExpirationMinutes=60

# ========================================
# DATABASE CONFIGURATION (REQUIRED)
# ========================================

# Admin database (metadata, tenants, plans, features)
ConnectionStrings__AdminDb=Host=your-postgres-host;Port=5432;Username=user;Password=password;Database=ecommerce_admin;SSL Mode=Require

# Tenant database template (use {DbName} placeholder)
Tenancy__TenantDbTemplate=Host=your-postgres-host;Port=5432;Username=user;Password=password;Database={DbName};SSL Mode=Require

# ========================================
# CORS CONFIGURATION (OPTIONAL)
# ========================================

# Allowed frontend origins (comma-separated)
# Example: https://myapp.com,http://localhost:4200
AllowedOrigins=https://pwaecommercee.netlify.app,http://localhost:4200,http://localhost:3000,http://localhost:5173

# ========================================
# GOOGLE CLOUD STORAGE (OPTIONAL)
# ========================================

GoogleStorage__BucketName=your-firebase-bucket.appspot.com

# ========================================
# LOGGING (OPTIONAL)
# ========================================

Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning
Logging__LogLevel__Microsoft.EntityFrameworkCore=Warning

# ========================================
# NOTES
# ========================================
#
# 1. Copy this file to .env and update values
# 2. NEVER commit .env to Git (already in .gitignore)
# 3. In production, use secure secret management:
#    - Azure Key Vault
#    - AWS Secrets Manager
#    - Docker Secrets
#    - Kubernetes Secrets
#
# 4. For local development:
#    - Use User Secrets (dotnet user-secrets)
#    - Or environment variables
#    - Or appsettings.Development.json (NOT committed)
#
# 5. Rotate JWT SigningKey every 90 days
# 6. Use different keys per environment (dev/staging/prod)
